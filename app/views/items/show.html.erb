
<div class="container mt-4">
  <div class="row">
    <div class="col text-center">
      <h1><%= @item.title %></h1>
    </div>
  </div>

  <div class="card mb-3 border-primary border-left-0">
    <div class="row">
      <div class="col">
        <div class="card-body">
          <p class="card-text"><strong>Title : </strong><%= @item.title %></p>
          <p class="card-text"><strong>Description : </strong><%= @item.description %></p>
          <p class="card-text"><strong>Prix : </strong><%= @item.price %> €</p>
       </div>
      </div>
    </div>
  </div>
</div>